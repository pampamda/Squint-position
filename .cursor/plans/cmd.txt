Train:

python train_squint.py \
    --env_id=SO101LiftCube-v1 \
    --num_envs=512 \
    --track \
    --wandb_entity=YOUR_WANDB_USERNAME

python train_squint_pos.py \
    --num_envs=512 \
    --num_eval_envs=8 \
    --render_size=96 \
    --track \
    --wandb_entity=panlinyu26-national-university-of-singapore-students-union


python deploy.py \
    --checkpoint=wandb \
    --env_id=SO101LiftCube-v1 \
    --wandb_entity=panlinyu26-national-university-of-singapore-students-union

python deploy.py \
    --checkpoint=/home/linyu/squint/runs/baseline/ckpt.pt \
    --env_id=SO101LiftCube-v1


python deploy_pos.py \
  --checkpoint runs/ckpt.pt \   # checkpoint 路径
  --episodes 5 \                # 跑几个 episode（默认5）
  --max-episode-steps 120 \     # 每个 episode 最多多少步（默认120）
  --action-scale 0.15 \         # 动作缩放系数，越小越慢越安全（默认0.15）
  --control-freq 30 \           # 目标控制频率 Hz（默认30）
  --seed 1                      # 随机种子（默认1）